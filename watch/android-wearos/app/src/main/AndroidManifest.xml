<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />

    <!-- androidx.wear.watchface holds a wake lock while rendering in interactive mode -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_1440"
        android:label="1440 Planner"
        android:supportsRtl="true">

        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />

        <!-- Companion of the phone app, not standalone: it only shows what the phone sends. -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="false" />

        <!--
          Canvas watch face (androidx.wear.watchface Renderer.CanvasRenderer2).
          android:name resolves against the manifest package = the `namespace` in build.gradle,
          i.e. com.planner1440.watchface.WatchFaceService — our class, not the androidx base
          it shadows.
        -->
        <service
            android:name=".WatchFaceService"
            android:exported="true"
            android:label="1440 Planner"
            android:permission="android.permission.BIND_WALLPAPER">
            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService" />
                <category android:name="com.google.android.wearable.watchface.category.WATCH_FACE" />
            </intent-filter>
            <meta-data
                android:name="com.google.android.wearable.watchface.preview"
                android:resource="@drawable/watch_face_preview" />
            <meta-data
                android:name="com.google.android.wearable.watchface.preview_circular"
                android:resource="@drawable/watch_face_preview" />
            <!-- WallpaperService requires this to be a <wallpaper/> XML, so it is NOT
                 xml/watch_face_info (that one is the Watch Face Format descriptor). -->
            <meta-data
                android:name="android.service.wallpaper"
                android:resource="@xml/watch_face" />
        </service>

        <!-- Complication data sources: read the last snapshot from SharedPreferences "1440_watch" -->
        <service
            android:name=".MinuteCounterComplicationService"
            android:exported="true"
            android:icon="@drawable/ic_1440"
            android:label="1440 minute counter"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER">
            <intent-filter>
                <action android:name="android.support.wearable.complications.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>
            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="SHORT_TEXT" />
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="60" />
        </service>

        <service
            android:name=".NextBlockComplicationService"
            android:exported="true"
            android:icon="@drawable/ic_1440"
            android:label="1440 next block"
            android:permission="com.google.android.wearable.permission.BIND_COMPLICATION_PROVIDER">
            <intent-filter>
                <action android:name="android.support.wearable.complications.ACTION_COMPLICATION_UPDATE_REQUEST" />
            </intent-filter>
            <meta-data
                android:name="android.support.wearable.complications.SUPPORTED_TYPES"
                android:value="SHORT_TEXT" />
            <meta-data
                android:name="android.support.wearable.complications.UPDATE_PERIOD_SECONDS"
                android:value="60" />
        </service>

        <!-- Wearable Data Layer listener: DataItems and Messages under /1440 from the phone -->
        <service
            android:name=".DataLayerClient"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data
                    android:host="*"
                    android:pathPrefix="/1440"
                    android:scheme="wear" />
            </intent-filter>
        </service>

    </application>
</manifest>
